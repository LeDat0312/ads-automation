# Code để pull Dashboard làm trang chính trên VPS

## Thay đổi:
1. ✅ Router prefix đã là `/dashboard` (không còn `/api/dashboard`)
2. ✅ Đã xóa redirect `/dashboard/` sang `/api/dashboard/` trong `main.py`
3. ✅ Đã sửa lỗi f-string trong `dashboard.py` (user_menu được chèn bằng string concatenation)

## Lệnh pull code:

```bash
cd /home/adsuser/ads-automation && git stash && git pull origin main && python3 -m py_compile app/main.py app/api/routes/dashboard.py && sudo supervisorctl restart all && sudo supervisorctl status
```

## Kiểm tra logs nếu vẫn lỗi:

```bash
# Xem logs chi tiết
sudo supervisorctl tail -200 ads-automation-api | grep -A 10 -B 10 "Error\|Exception\|Traceback"

# Hoặc xem toàn bộ logs
sudo supervisorctl tail -200 ads-automation-api

# Kiểm tra import
cd /home/adsuser/ads-automation
python3 -c "
try:
    from app.main import app
    from app.api.routes.dashboard import router
    print('✅ Import OK')
    # Kiểm tra routes
    routes = [r.path for r in app.routes if 'dashboard' in r.path]
    print('Routes có chứa dashboard:')
    for r in routes:
        print(f'  - {r}')
except Exception as e:
    print(f'❌ Lỗi: {e}')
    import traceback
    traceback.print_exc()
"
```

## Sau khi pull:
- Truy cập: `https://updatemetaads.site/dashboard/` (trang chính, không còn redirect)
- API endpoints:
  - `/dashboard/data` - Lấy dữ liệu dashboard
  - `/dashboard/filters` - Lấy filters (accounts, prefixes)

## Nếu vẫn lỗi:
Gửi output của lệnh kiểm tra logs để tôi có thể sửa lỗi cụ thể.


