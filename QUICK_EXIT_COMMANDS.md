# ⚡ PHÍM TẮT & LỆNH THOÁT NHANH

## 🐍 THOÁT VENV

### **Không có phím tắt, chỉ có lệnh:**

```bash
deactivate
```

**Không có:** Ctrl+C, Ctrl+D, Esc, etc.

---

## 📝 CÁC PHÍM TẮT HỮU ÍCH KHÁC

### **1. Thoát Python interactive shell:**
- `Ctrl+D` - Exit Python shell
- Hoặc: `exit()`

### **2. Thoát nano editor:**
- `Ctrl+X` - Exit nano
- `Ctrl+O` - Save (Write Out)
- `Ctrl+K` - Cut line

### **3. Thoát vim editor:**
- `Esc` → `:q` - Quit
- `Esc` → `:wq` - Save and quit
- `Esc` → `:q!` - Quit without saving

### **4. Dừng lệnh đang chạy:**
- `Ctrl+C` - Interrupt/Cancel command
- `Ctrl+Z` - Suspend (pause) process

### **5. Clear terminal:**
- `Ctrl+L` - Clear screen
- Hoặc: `clear`

### **6. Thoát SSH session:**
- `exit` - Exit SSH
- Hoặc: `logout`
- Hoặc: `Ctrl+D`

---

## 🚀 QUICK REFERENCE

| Tình huống | Phím tắt/Lệnh |
|------------|---------------|
| Thoát venv | `deactivate` |
| Thoát Python shell | `Ctrl+D` hoặc `exit()` |
| Thoát nano | `Ctrl+X` |
| Thoát vim | `Esc` → `:q` |
| Dừng lệnh | `Ctrl+C` |
| Clear screen | `Ctrl+L` |
| Thoát SSH | `exit` hoặc `Ctrl+D` |

---

## 💡 TIPS

### **Nếu muốn nhanh:**

1. **Gõ `deactivate` và Enter** - Nhanh nhất
2. **Dùng tab completion:** Gõ `dea` + `Tab` → `deactivate`
3. **Tạo alias (tùy chọn):**
   ```bash
   # Thêm vào ~/.bashrc
   alias venv-off='deactivate'
   
   # Reload
   source ~/.bashrc
   
   # Dùng
   venv-off
   ```

---

## ✅ TÓM TẮT

**Thoát venv:** Chỉ có lệnh `deactivate`, không có phím tắt.

**Cách nhanh nhất:** Gõ `deactivate` và Enter.

---

**Chỉ cần gõ `deactivate` và Enter! 🚀**


