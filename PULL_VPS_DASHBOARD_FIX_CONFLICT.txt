cd ~/ads-automation

# Option 1: Stash local changes (giữ lại để sau này có thể apply lại nếu cần)
git stash
git pull origin main
python3 -m py_compile app/api/routes/dashboard.py
sudo supervisorctl restart ads-automation
sleep 3
sudo supervisorctl status

# Nếu muốn xem lại local changes đã stash:
# git stash list
# git stash show -p

# Nếu muốn discard local changes hoàn toàn (xóa bỏ), dùng lệnh này thay vì git stash:
# git checkout -- app/api/routes/dashboard.py
# git pull origin main

