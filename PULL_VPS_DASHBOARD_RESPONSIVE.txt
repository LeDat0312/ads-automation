# Script để pull code Dashboard với Responsive Design cải thiện lên VPS
# Chạy các lệnh sau trên VPS:

cd ~/ads-automation
git stash
git pull origin main
python3 -m py_compile app/api/routes/dashboard.py app/main.py
sudo supervisorctl restart all
sudo supervisorctl status

# Kiểm tra logs nếu có lỗi:
# tail -f /var/log/supervisor/ads-automation-api.log

# Cải thiện Responsive Design:
# - Mobile filter toggle button
# - Optimized stats grid (2 columns on tablet, 1 column on mobile)
# - Optimized prefix grid (1 column on mobile)
# - Improved table scrolling với touch support
# - Enhanced header với mobile-friendly layout
# - Better button sizes và spacing trên mobile
# - Improved date picker trên mobile

