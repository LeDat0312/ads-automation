# 🖥️ VPS SETUP GUIDE - HƯỚNG DẪN CHI TIẾT

## 🎯 KHUYẾN NGHỊ VPS

### **🏆 TOP 3 LỰA CHỌN:**

---

## **1. DIGITALOCEAN** ⭐⭐⭐⭐⭐ (KHUYẾN NGHỊ)

### **✅ ƯU ĐIỂM:**
- ✅ **Giá rẻ:** $5-20/tháng
- ✅ **Dễ dùng:** UI đơn giản, dễ quản lý
- ✅ **Ổn định:** Uptime 99.99%
- ✅ **Nhiều data center:** Singapore, Frankfurt, New York, etc.
- ✅ **Tài liệu tốt:** Nhiều tutorials và guides
- ✅ **Community lớn:** Nhiều developers sử dụng
- ✅ **Free credits:** $200 cho new users (60 ngày)

### **💰 GIÁ CẢ:**
- **Basic:** $5/tháng (1 CPU, 1GB RAM, 25GB SSD)
- **Professional:** $12/tháng (2 CPU, 2GB RAM, 50GB SSD) ⭐ **KHUYẾN NGHỊ**
- **Professional:** $24/tháng (4 CPU, 4GB RAM, 100GB SSD)

### **🌍 DATA CENTER:**
- **Singapore** (Gần Việt Nam nhất) ⭐ **KHUYẾN NGHỊ**
- **Frankfurt** (Châu Âu)
- **New York** (Châu Mỹ)
- **San Francisco** (Châu Mỹ)

### **🔗 LINK:**
- Website: https://www.digitalocean.com
- Referral: https://m.do.co/c/your-referral-code (Nhận $200 free)

---

## **2. LINODE** ⭐⭐⭐⭐⭐

### **✅ ƯU ĐIỂM:**
- ✅ **Giá rẻ:** $5-20/tháng
- ✅ **Hiệu năng tốt:** CPU và RAM mạnh
- ✅ **Ổn định:** Uptime 99.99%
- ✅ **Nhiều data center:** Tokyo, Singapore, Frankfurt, etc.
- ✅ **Tài liệu tốt:** Nhiều tutorials

### **💰 GIÁ CẢ:**
- **Nanode:** $5/tháng (1 CPU, 1GB RAM, 25GB SSD)
- **Linode 2GB:** $12/tháng (1 CPU, 2GB RAM, 50GB SSD) ⭐ **KHUYẾN NGHỊ**
- **Linode 4GB:** $24/tháng (2 CPU, 4GB RAM, 80GB SSD)

### **🌍 DATA CENTER:**
- **Singapore** (Gần Việt Nam nhất) ⭐ **KHUYẾN NGHỊ**
- **Tokyo** (Châu Á)
- **Frankfurt** (Châu Âu)
- **Newark** (Châu Mỹ)

### **🔗 LINK:**
- Website: https://www.linode.com
- Referral: https://www.linode.com/lp/refer/?r=your-referral-code

---

## **3. HETZNER** ⭐⭐⭐⭐

### **✅ ƯU ĐIỂM:**
- ✅ **Giá rất rẻ:** €4-20/tháng (khoảng $5-25)
- ✅ **Hiệu năng tốt:** CPU và RAM mạnh
- ✅ **Ổn định:** Uptime 99.99%
- ✅ **Nhiều data center:** Đức, Phần Lan

### **⚠️ NHƯỢC ĐIỂM:**
- ⚠️ **Data center xa Việt Nam:** Chỉ có ở Châu Âu
- ⚠️ **UI phức tạp hơn:** Khó dùng hơn DigitalOcean
- ⚠️ **Tài liệu ít hơn:** Ít tutorials bằng tiếng Anh

### **💰 GIÁ CẢ:**
- **CX11:** €4/tháng (1 CPU, 2GB RAM, 20GB SSD)
- **CX21:** €6/tháng (2 CPU, 4GB RAM, 40GB SSD) ⭐ **KHUYẾN NGHỊ**
- **CX31:** €12/tháng (2 CPU, 8GB RAM, 80GB SSD)

### **🌍 DATA CENTER:**
- **Falkenstein** (Đức)
- **Helsinki** (Phần Lan)
- **Nuremberg** (Đức)

### **🔗 LINK:**
- Website: https://www.hetzner.com
- Cloud: https://www.hetzner.com/cloud

---

## **4. VULTR** ⭐⭐⭐⭐

### **✅ ƯU ĐIỂM:**
- ✅ **Giá rẻ:** $5-20/tháng
- ✅ **Nhiều data center:** Singapore, Tokyo, Seoul, etc.
- ✅ **Hiệu năng tốt:** CPU và RAM mạnh
- ✅ **Ổn định:** Uptime 99.99%

### **💰 GIÁ CẢ:**
- **Regular Performance:** $5/tháng (1 CPU, 1GB RAM, 25GB SSD)
- **Regular Performance:** $12/tháng (2 CPU, 2GB RAM, 55GB SSD) ⭐ **KHUYẾN NGHỊ**
- **Regular Performance:** $24/tháng (4 CPU, 4GB RAM, 100GB SSD)

### **🌍 DATA CENTER:**
- **Singapore** (Gần Việt Nam nhất) ⭐ **KHUYẾN NGHỊ**
- **Tokyo** (Châu Á)
- **Seoul** (Châu Á)
- **Los Angeles** (Châu Mỹ)

### **🔗 LINK:**
- Website: https://www.vultr.com

---

## 📊 SO SÁNH CÁC VPS

| VPS | Giá/tháng | CPU | RAM | SSD | Data Center | Đánh giá |
|-----|-----------|-----|-----|-----|-------------|----------|
| **DigitalOcean** | $12 | 2 | 2GB | 50GB | Singapore | ⭐⭐⭐⭐⭐ |
| **Linode** | $12 | 1 | 2GB | 50GB | Singapore | ⭐⭐⭐⭐⭐ |
| **Hetzner** | €6 ($7) | 2 | 4GB | 40GB | Đức | ⭐⭐⭐⭐ |
| **Vultr** | $12 | 2 | 2GB | 55GB | Singapore | ⭐⭐⭐⭐ |

---

## 🎯 KHUYẾN NGHỊ CẤU HÌNH

### **CHO HỆ THỐNG PYTHON + FASTAPI + POSTGRESQL:**

#### **🔹 CẤU HÌNH TỐI THIỂU (Development/Testing):**
- **CPU:** 1-2 cores
- **RAM:** 2GB
- **SSD:** 25-50GB
- **Bandwidth:** 1-2TB
- **Giá:** $5-12/tháng

#### **🔹 CẤU HÌNH KHUYẾN NGHỊ (Production):**
- **CPU:** 2-4 cores ⭐
- **RAM:** 4GB ⭐
- **SSD:** 50-100GB ⭐
- **Bandwidth:** 2-4TB
- **Giá:** $12-24/tháng

#### **🔹 CẤU HÌNH CAO CẤP (Scale lớn):**
- **CPU:** 4-8 cores
- **RAM:** 8-16GB
- **SSD:** 100-200GB
- **Bandwidth:** 4-8TB
- **Giá:** $24-48/tháng

---

## 🖥️ HỆ ĐIỀU HÀNH

### **🏆 KHUYẾN NGHỊ: UBUNTU 22.04 LTS** ⭐⭐⭐⭐⭐

### **✅ ƯU ĐIỂM:**
- ✅ **Ổn định:** LTS (Long Term Support) - hỗ trợ 5 năm
- ✅ **Dễ dùng:** UI đơn giản, dễ quản lý
- ✅ **Tài liệu tốt:** Nhiều tutorials và guides
- ✅ **Community lớn:** Nhiều developers sử dụng
- ✅ **Security:** Được update thường xuyên
- ✅ **Compatible:** Hỗ trợ tốt Python, Docker, PostgreSQL

### **🔹 CÁC PHIÊN BẢN UBUNTU:**
- **Ubuntu 22.04 LTS** ⭐ **KHUYẾN NGHỊ** (Hỗ trợ đến 2027)
- **Ubuntu 20.04 LTS** (Hỗ trợ đến 2025)
- **Ubuntu 24.04 LTS** (Mới nhất, hỗ trợ đến 2029)

### **🔹 CÁC LỰA CHỌN KHÁC:**
- **Debian 12** ⭐⭐⭐⭐ (Ổn định, nhẹ)
- **CentOS Stream** ⭐⭐⭐ (Enterprise-grade)
- **AlmaLinux** ⭐⭐⭐ (RHEL-compatible)

---

## 🚀 HƯỚNG DẪN SETUP

### **BƯỚC 1: MUA VPS**

1. **Chọn nhà cung cấp:**
   - DigitalOcean (Khuyến nghị)
   - Linode
   - Hetzner
   - Vultr

2. **Chọn cấu hình:**
   - CPU: 2 cores
   - RAM: 4GB
   - SSD: 50GB
   - Bandwidth: 2TB

3. **Chọn data center:**
   - Singapore (Gần Việt Nam nhất)

4. **Chọn hệ điều hành:**
   - Ubuntu 22.04 LTS

5. **Tạo VPS:**
   - Đăng ký tài khoản
   - Tạo Droplet/Instance
   - Chọn cấu hình
   - Chọn hệ điều hành
   - Tạo VPS

---

### **BƯỚC 2: SETUP VPS**

#### **2.1. Kết nối VPS:**

```bash
# SSH vào VPS
ssh root@your-vps-ip

# Hoặc với user
ssh user@your-vps-ip
```

#### **2.2. Update hệ thống:**

```bash
# Update package list
sudo apt update

# Upgrade packages
sudo apt upgrade -y

# Reboot (nếu cần)
sudo reboot
```

#### **2.3. Setup Firewall:**

```bash
# Install UFW (Uncomplicated Firewall)
sudo apt install ufw -y

# Allow SSH
sudo ufw allow 22/tcp

# Allow HTTP
sudo ufw allow 80/tcp

# Allow HTTPS
sudo ufw allow 443/tcp

# Enable firewall
sudo ufw enable

# Check status
sudo ufw status
```

#### **2.4. Setup User (Không dùng root):**

```bash
# Tạo user mới
sudo adduser your-username

# Add user to sudo group
sudo usermod -aG sudo your-username

# Switch to new user
su - your-username

# Test sudo
sudo whoami
```

#### **2.5. Setup SSH Key:**

```bash
# Generate SSH key (trên máy local)
ssh-keygen -t rsa -b 4096 -C "your-email@example.com"

# Copy SSH key to VPS
ssh-copy-id your-username@your-vps-ip

# Test SSH key
ssh your-username@your-vps-ip
```

---

### **BƯỚC 3: INSTALL PYTHON**

#### **3.1. Install Python 3.11+:**

```bash
# Install dependencies
sudo apt install software-properties-common -y

# Add deadsnakes PPA
sudo add-apt-repository ppa:deadsnakes/ppa -y

# Update package list
sudo apt update

# Install Python 3.11
sudo apt install python3.11 python3.11-venv python3.11-dev -y

# Install pip
sudo apt install python3-pip -y

# Verify installation
python3.11 --version
pip3 --version
```

#### **3.2. Setup Python Virtual Environment:**

```bash
# Create virtual environment
python3.11 -m venv venv

# Activate virtual environment
source venv/bin/activate

# Upgrade pip
pip install --upgrade pip

# Install packages
pip install fastapi uvicorn sqlalchemy psycopg2-binary pydantic
```

---

### **BƯỚC 4: INSTALL POSTGRESQL**

#### **4.1. Install PostgreSQL:**

```bash
# Install PostgreSQL
sudo apt install postgresql postgresql-contrib -y

# Start PostgreSQL
sudo systemctl start postgresql

# Enable PostgreSQL (start on boot)
sudo systemctl enable postgresql

# Check status
sudo systemctl status postgresql
```

#### **4.2. Setup PostgreSQL:**

```bash
# Switch to postgres user
sudo -u postgres psql

# Create database
CREATE DATABASE your_database_name;

# Create user
CREATE USER your_username WITH PASSWORD 'your_password';

# Grant privileges
GRANT ALL PRIVILEGES ON DATABASE your_database_name TO your_username;

# Exit PostgreSQL
\q
```

#### **4.3. Configure PostgreSQL:**

```bash
# Edit PostgreSQL config
sudo nano /etc/postgresql/14/main/postgresql.conf

# Uncomment and set:
# listen_addresses = 'localhost'

# Edit pg_hba.conf
sudo nano /etc/postgresql/14/main/pg_hba.conf

# Add:
# local   all             all                                     md5
# host    all             all             127.0.0.1/32            md5

# Restart PostgreSQL
sudo systemctl restart postgresql
```

---

### **BƯỚC 5: INSTALL DOCKER**

#### **5.1. Install Docker:**

```bash
# Install dependencies
sudo apt install apt-transport-https ca-certificates curl gnupg lsb-release -y

# Add Docker GPG key
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg

# Add Docker repository
echo "deb [arch=amd64 signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

# Update package list
sudo apt update

# Install Docker
sudo apt install docker-ce docker-ce-cli containerd.io -y

# Start Docker
sudo systemctl start docker

# Enable Docker (start on boot)
sudo systemctl enable docker

# Check status
sudo systemctl status docker
```

#### **5.2. Setup Docker User:**

```bash
# Add user to docker group
sudo usermod -aG docker your-username

# Logout and login again
exit
ssh your-username@your-vps-ip

# Test Docker
docker run hello-world
```

#### **5.3. Install Docker Compose:**

```bash
# Download Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose

# Make executable
sudo chmod +x /usr/local/bin/docker-compose

# Verify installation
docker-compose --version
```

---

### **BƯỚC 6: INSTALL NGINX**

#### **6.1. Install Nginx:**

```bash
# Install Nginx
sudo apt install nginx -y

# Start Nginx
sudo systemctl start nginx

# Enable Nginx (start on boot)
sudo systemctl enable nginx

# Check status
sudo systemctl status nginx
```

#### **6.2. Configure Nginx:**

```bash
# Create Nginx config
sudo nano /etc/nginx/sites-available/your-domain.com

# Add configuration:
server {
    listen 80;
    server_name your-domain.com www.your-domain.com;

    location / {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Enable site
sudo ln -s /etc/nginx/sites-available/your-domain.com /etc/nginx/sites-enabled/

# Test Nginx config
sudo nginx -t

# Restart Nginx
sudo systemctl restart nginx
```

---

### **BƯỚC 7: SETUP SSL (LET'S ENCRYPT)**

#### **7.1. Install Certbot:**

```bash
# Install Certbot
sudo apt install certbot python3-certbot-nginx -y

# Get SSL certificate
sudo certbot --nginx -d your-domain.com -d www.your-domain.com

# Test renewal
sudo certbot renew --dry-run
```

---

## 📋 CHECKLIST SETUP

### **✅ SAU KHI SETUP XONG:**

- [ ] VPS đã được tạo và chạy
- [ ] Đã SSH vào VPS thành công
- [ ] Đã update hệ thống
- [ ] Đã setup firewall (UFW)
- [ ] Đã tạo user mới (không dùng root)
- [ ] Đã setup SSH key
- [ ] Đã install Python 3.11+
- [ ] Đã install PostgreSQL
- [ ] Đã setup PostgreSQL database và user
- [ ] Đã install Docker
- [ ] Đã install Docker Compose
- [ ] Đã install Nginx
- [ ] Đã setup Nginx reverse proxy
- [ ] Đã setup SSL (Let's Encrypt)
- [ ] Đã test tất cả services

---

## 🔒 SECURITY BEST PRACTICES

### **1. Firewall:**
```bash
# Allow only necessary ports
sudo ufw allow 22/tcp    # SSH
sudo ufw allow 80/tcp    # HTTP
sudo ufw allow 443/tcp   # HTTPS
sudo ufw enable
```

### **2. SSH Security:**
```bash
# Disable root login
sudo nano /etc/ssh/sshd_config
# Set: PermitRootLogin no

# Change SSH port (optional)
# Set: Port 2222

# Restart SSH
sudo systemctl restart sshd
```

### **3. Fail2Ban:**
```bash
# Install Fail2Ban
sudo apt install fail2ban -y

# Start Fail2Ban
sudo systemctl start fail2ban
sudo systemctl enable fail2ban

# Check status
sudo systemctl status fail2ban
```

### **4. Automatic Updates:**
```bash
# Install unattended-upgrades
sudo apt install unattended-upgrades -y

# Enable automatic updates
sudo dpkg-reconfigure -plow unattended-upgrades
```

---

## 💰 COST ESTIMATION

### **VPS COST:**
- **DigitalOcean:** $12/tháng (2 CPU, 4GB RAM, 50GB SSD)
- **Domain:** $10-15/năm
- **SSL:** Free (Let's Encrypt)
- **Total:** $12/tháng + $10-15/năm

### **ADDITIONAL COST:**
- **Backup:** $2-5/tháng (optional)
- **Monitoring:** $0-10/tháng (optional)
- **CDN:** $0-20/tháng (optional)

---

## 🎯 KẾT LUẬN

### **KHUYẾN NGHỊ:**
1. **VPS:** DigitalOcean ($12/tháng)
2. **Cấu hình:** 2 CPU, 4GB RAM, 50GB SSD
3. **Data Center:** Singapore
4. **Hệ điều hành:** Ubuntu 22.04 LTS
5. **Domain:** Mua từ Namecheap hoặc Cloudflare ($10-15/năm)

### **TỔNG CHI PHÍ:**
- **VPS:** $12/tháng
- **Domain:** $10-15/năm
- **SSL:** Free
- **Total:** ~$12/tháng

---

**Chúc bạn setup thành công! 🚀**

